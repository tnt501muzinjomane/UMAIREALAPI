<%= form_for(rtable) do |f| %>
  <% if rtable.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(rtable.errors.count, "error") %> prohibited this rtable from being saved:</h2>

      <ul>
      <% rtable.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :min_guest %>
    <%= f.number_field :min_guest %>
  </div>

  <div class="field">
    <%= f.label :max_guest %>
    <%= f.number_field :max_guest %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
